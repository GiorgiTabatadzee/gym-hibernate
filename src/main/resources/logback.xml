<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Business logic: informative. -->
    <logger name="com.epam.gym" level="INFO"/>

    <!-- Hibernate SQL is verbose; drop to WARN to keep console readable.
         Flip to DEBUG locally if you need to see generated SQL. -->
    <logger name="org.hibernate" level="WARN"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
